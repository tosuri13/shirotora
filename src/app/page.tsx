import { StationBoard } from "@/components/StationBoard";

export default function AppPage() {
  return (
    <div className="px-[16px] py-[16px] lg:px-[32px] lg:py-[24px]">
      <StationBoard />
    </div>
  );
}
